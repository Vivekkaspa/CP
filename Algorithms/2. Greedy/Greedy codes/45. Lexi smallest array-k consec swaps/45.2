#include<stdio.h>
#include<string.h>

int main()
{
    int n, k;
    scanf("%d %d", &n, &k);
    
    char arr[n][20]; // Assuming each string has a maximum length of 20 characters
    for(int i=0; i<n; i++)
    {
        scanf("%s", arr[i]);
    }
    
    int i = 0;
    while(k > 0 && i < n)
    {
        int minIndex = i;
        for(int j=i+1; j<=min(i+k, n-1); j++)
        {
            if(strcmp(arr[j], arr[minIndex]) < 0)
            {
                minIndex = j;
            }
        }
        if(minIndex != i)
        {
            char temp[20];
            strcpy(temp, arr[i]);
            strcpy(arr[i], arr[minIndex]);
            strcpy(arr[minIndex], temp);
            if(strcmp(arr[i], arr[i-1]) == 0)
            {
                k--;
            }
            else
            {
                k -= 2;
            }
        }
        i++;
    }
    
    for(int i=0; i<n; i++)
    {
        printf("%s ", arr[i]);
    }
    printf("\n");
    
    return 0;
}
