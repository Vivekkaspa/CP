function binomial_coefficient(n, k):
    if k > n:
        return 0
    if k == 0 or k == n:
        return 1
    if memo[n][k] is not empty:
        return memo[n][k]
    memo[n][k] = binomial_coefficient(n-1, k-1) + binomial_coefficient(n-1, k)
    return memo[n][k]

# initialize the memoization table with -1 for empty entries
memo = []
for i in range(n+1):
    memo.append([-1]*(k+1))

# call the function with the desired inputs
result = binomial_coefficient(n, k)
